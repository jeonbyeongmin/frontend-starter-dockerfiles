# Frontend starter dockerfiles

### Usage

```shell
$ mv docker-compose.yaml <your project directory>
```

```shell
$ docker-compose run --rm <service-name>
```

### Example

```shell
$ docker-compose run --rm next
```
